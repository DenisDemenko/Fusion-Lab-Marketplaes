# План міграції: злиття старого сайту, маркетплейсу і Book_Creality

Узгоджено через два раунди уточнень 2026-08-29. Цей документ — план робіт;
обґрунтування архітектурних рішень піде в ADR 0007 (перше завдання плану).

Мета: звести три системи в один продукт для реальних користувачів —
статичний грантовий сайт (`site/`), маркетплейс (`marketplace/`) і
AI-конструктор книг Book_Creality — зберігши робочий код кожної.

## Прийняті рішення

| Питання | Рішення |
|---|---|
| Глибина злиття з Book_Creality | **Окремий сервіс + SSO + єдиний домен** (не переписуємо 62k рядків) |
| Автентифікація | **Firebase Auth** (та, що в маркетплейсі). Nova втрачає власні паролі/сесії |
| Ролі | 8 ролей, **одна на користувача**, обирається при реєстрації; адмін точково коригує права |
| Відео курсів | **YouTube unlisted**, гейтинг за наявним `Entitlement` |
| Дизайн-система | Застосовуємо до **всіх** сторінок; акцент — за категорією лістингу |
| Логотипи УКФ/ЄС | **Прибираємо** |
| Підписки | Переносимо для викладачів, письменників, експертів, маркетологів, студентів |
| Дедлайн | Немає |

## Що вже зроблено (не входить в обсяг)

Перевірено в коді перед плануванням — це економить значну частину роботи:

- **Усі 10 курсів уже в маркетплейсі** зі структурою модулів, занять, цілей,
  тем, практики й проєктів. Текст і структура збережені — переносити нічого.
- **`Entitlement`** уже працює: відкриває завантаження платних файлів після
  оплати. Той самий механізм візьме на себе гейтинг відео.
- **Медіа-інфраструктура** готова: `MediaAsset` з `kind` (cover/attachment) і
  `access` (public/entitled), завантаження через `POST /seller/listings/:id/media`,
  UI-компонент `ListingMediaManager`. Обкладинки курсів лягають сюди без нової
  підсистеми — потрібне лише розширення під зображення уроків.
- **i18n UA/EN** на всіх 27 маршрутах — нові сторінки просто дотримуються патерну.
- **API-міст** `POST /bridge/books` уже публікує книги з Nova в каталог.

## Фази

Розмір: **S** — дні, **M** — 1–2 тижні, **L** — 3–4 тижні, **XL** — понад місяць.
Оцінки орієнтовні й стосуються обсягу роботи, не календаря.

---

### Фаза A — Фундамент: ролі й права ✅ завершено 2026-08-30

Йшла першою, бо від матриці прав залежать усі наступні фази. Деталі рішень —
[`docs/adr/0007-role-permission-model.md`](adr/0007-role-permission-model.md).

- [x] **A0.** ADR 0007: модель ролей і прав, ґейт `sales:access` за
      `salesApproved`, `roleChosenAt` для одноразового самовибору.
- [x] **A1.** `UserRole` розширено з 3 до 8: `buyer`, `seller`, `admin`,
      `writer`, `expert`, `sales_manager`, `instruction_engineer`, `student`.
- [x] **A2.** Матриця прав — з відхиленням від початкового формулювання:
      каталог прав (`Permission`) лишився в коді (`src/auth/permissions.ts`),
      **не** окремою таблицею — жоден екран не дає адміну вигадати нове
      право на льоту, лише дозволити/заборонити вже наявне конкретній
      людині. У БД — тільки персональні відхилення
      (`UserPermissionOverride`), і фактичний ґейт `sales:access` за
      `User.salesApproved`, що закриває ризик нижче. Обґрунтування — ADR 0007.
- [x] **A3.** Guard-шар — з відхиленням: `RolesGuard`/`@Roles('admin')` на
      наявних адмінських маршрутах **не чіпали** (уже коректні й стабільні,
      тотальний рефакторинг ризикував регресією без функціональної
      потреби). Новий `PermissionsGuard`/`@RequirePermissions(...)` —
      адитивний шар для тонких прав нових ролей (`POST /me/role`,
      `PATCH /admin/users/:id/permissions`, `.../sales-approval`).
- [x] **A4.** `POST /me/role` (одноразовий самовибір) + `RoleGate` на
      фронтенді (блокує застосунок до вибору ролі для нового акаунту) +
      розширена `/admin/users` (роль, перемикач `salesApproved`,
      розгортна панель ефективних прав і оверрайдів).

**Знайдено й виправлено під час реалізації, не в початковому плані:**
свіжий рядок `User` створюється зі схемним дефолтом `buyer` ще до вибору
ролі — без окремого прапорця `RoleGate` заблокував би **і сідованого
адміна** на екрані вибору ролі назавжди. Виправлено `roleChosenAt` +
бекфіл-міграцією для наявних акаунтів + `prisma/seed.ts` тепер сам
проставляє це поле для адміна/продавця лабораторії.

**Права по ролях (реалізовано):**

| Роль | Може |
|---|---|
| Письменник (`writer`) | `books:write` + `sales:access` (за схваленням) |
| Менеджер продажів (`sales_manager`) | `sales:access` (за схваленням) — без `listings:write` |
| Експерт (`expert`) | `listings:write` — створює курси через **наявну** форму лістингу в кабінеті продавця. Підтверджено e2e-тестом: кабінет продавця гейтиться за `SellerProfile.status`, не за роллю, тож жодних змін коду не знадобилось |
| `instruction_engineer`, `student` | порожній пресет — права не визначені планом, додаються через `UserPermissionOverride` вручну до узгодження |

✅ **Ризик закрито:** `sales:access` пропонується пресетом ролі, але реально
працює лише після `PATCH /admin/users/:id/sales-approval` — та сама межа
довіри, що вже існує для `SellerProfile.status`. Адмін виключений з цього
ґейту явно (роль і так означає повну довіру).

**Перевірено:** 12 нових e2e-тестів (самовибір ролі, одноразовість,
ґейт `sales:access`, оверрайди прав у обидва боки, `expert` через наявний
кабінет продавця), повний прогін 84 e2e + 10 unit — усі зелені; typecheck,
eslint, build обох застосунків — чисті.

---

### Фаза B — Дизайн-система `XL` — B1–B4, B6 завершено 2026-08-30, B5 триває хвилями, B7 неактуальний

- [x] **B1.** Токени: палітра (`#F4ECE1` основа, `#FFFFFF` картки, `#2B1A10`
      текст), два акценти — «Ремесло» `#C97F4F` і «Технології» `#59637E`,
      системні кольори станів. Реалізовано в `globals.css` під наявними
      іменами змінних (`--background`, `--foreground`, `--muted`,
      `--accent`/`--accent-soft`) — 28 файлів, що вже читали ці токени,
      підхопили нову палітру без жодної правки.
- [x] **B2.** Шрифти через `next/font/google`: Unbounded (лише заголовки,
      глобальне правило `h1–h6`), Golos Text (контент/UI), JetBrains Mono
      (технічні дані). Geist лишився в стеку фолбеком, не видалений.
- [x] **B3.** Крок сітки — вже Tailwind-дефолт (4px), окремого токена не
      знадобилось. Контейнер 1120px — не вводили окремим класом, лишили
      наявні `max-w-5xl`/`max-w-6xl` для стабільності верстки; повернутись
      у B5.
- [x] **B4.** Компоненти оновлено глобально в `globals.css`: кнопки
      (мін. 44px/48px sm:, одна первинна форма), `.input`, `.label`,
      `.badge`, `.card`, `.section-title` — усі 27 сторінок отримали нову
      палітру одразу через ці спільні класи.
- ⏳ **B5.** **Редизайн усіх 27 сторінок** — свідомо не за один прохід (як і
      попереджав план). B1–B4 вже дають візуальний зсув скрізь через спільні
      класи й токени; окремо зроблено перший крок B5 — заміна нейтральних
      `text-zinc-*`/`bg-zinc-*`/`border-zinc-*` на нові токени одним
      скриптованим проходом по 40 файлах (семантичні кольори станів —
      `red-*`/`emerald-*`/`amber-*` — не чіпали). Власне поглиблений
      редизайн — макет, копірайтинг, компонування — лишається робити
      сторінка за сторінкою, коли дійде черга.
- [x] **B6.** Акцент за категорією лістингу: `.accent-tech` клас
      (`src/lib/category-accent.ts`) на `ListingCard` і сторінці деталей —
      `bpla`/`chpu`/`3d-druk` → tech, решта (`mebli`/`art`/`osvita`/`vyroby`/
      `kursy`) → craft (дефолт). Перевірено вживу: сторінка ЧПУ-курсу
      показує `--accent: #59637e`, арт-терапії — `#c97f4f`.
- ➖ **B7.** Неактуально — перевірив `site-footer.tsx`: він написаний з нуля
      у Фазі 1 і ніколи не мав логотипів УКФ/ЄС. Прибирати нема чого.

---

### Фаза C — Перший екран `M` — завершено 2026-08-30

- [x] **C1.** Hero зі старого сайту **над** наявним hero маркетплейсу
      (`LabHero`, окремий `<h1>`; наявний заголовок маркетплейсу став
      `<h2>` — на сторінці має бути лише один `h1`). Текст і CTA — старі.
      Куди ведуть кнопки (не було визначено планом, вирішив сам, щоб не
      зупинятись): «Записатися на спільне заняття» → `/catalog?kind=course`
      (бронювання з Фази F2 ще нема), «Переглянути напрямки» → якір на
      секцію категорій, що вже є нижче на цій самій сторінці, «Вхід у
      сервіс» → `/login`. ~~Переглянути обидва тимчасові маршрути, коли
      з'являться F2/F3~~ — «Записатися на спільне заняття» переведено на
      реальний `/schedule` у Фазі F2; «Переглянути напрямки» лишається
      якорем — окремої сторінки напрямків план не передбачає (F3 не
      робили окремою формою, див. Фазу F).
- [x] **C2.** `light-rays` портовано як React-компонент (`light-rays.tsx`) на
      **опублікованому npm-пакеті `ogl`**, не через копіювання 300+
      файлів вендор-теки старого сайту. Той самий шейдер і WebGL-логіка.
      `prefers-reduced-motion` — компонент узагалі не монтує WebGL.
      Перевірено вживу: canvas реально рендериться (1265×765), без помилок
      у консолі.
- [x] **C3.** Динамічна заливка: базовий `linear-gradient` (менше покриття
      праворуч, ніж в оригіналі) + `radial-gradient`, що йде за курсором
      через CSS-змінні `--mx`/`--my`, оновлювані на `pointermove`.
      Перевірено вживу: симуляція `pointermove` змінює `--mx` з 30% (дефолт)
      на реальну позицію курсора.
- [x] **C4.** `logo_site.png` (2.1 МБ) → `hero-lab.webp` (127 КБ) +
      `hero-lab.avif` (87 КБ) через `sharp`, `<picture>` з фолбеком.
- [x] **C5.** Двомовність hero через `next-intl` (простір `labHero`).
      Знайдено й виправлено пастку: `t.rich("title", {br: ...})` не
      парсив самозакривний `<br/>` у повідомленні — ICU/next-intl очікує
      парний тег `<br></br>`. Виявлено саме живою перевіркою в браузері
      (DOM показував буквальний текст `<br/>`), не типчеком.
- [x] **C6.** Смужку `hero-facts` не перенесено.

---

### Фаза D — Курси: відео, прогрес, зображення `M` — завершено 2026-08-30

- [x] **D1.** `videoUrl` додано в `shared-types` (`CourseLesson.videoUrl`),
      і — окремо від початкового формулювання плану — зроблено **редагованим
      через текстовий формат програми курсу**, не лише читаним із seed-даних:
      рядок `video: <посилання>` одразу під заняттям (`curriculum-text.ts`).
      **Знайдено й закрито дірку безпеки під час реалізації:** публічний
      `GET /catalog/:slug` віддавав `videoUrl` у сирому JSON будь-кому
      неавтентифікованому — той самий клас проблеми, що вже розвʼязаний
      для `lockedMedia`, просто ще не застосований до відео. Тепер
      `toListingDetail` стрипає `videoUrl` (`stripVideoUrls` у
      `listing.mapper.ts`), лишаючи заголовки занять видимими.
- [x] **D2.** Гейтинг за `Entitlement`: `GET /me/library/:slug` (уже існував
      бекенд-методом, але жодна сторінка фронтенду його не використовувала)
      віддає повну програму з `videoUrl` лише після перевірки
      `Entitlement`; нова сторінка `/account/library/[slug]` рендерить
      `youtube-nocookie`-плеєр. Без покупки — 404, не урізана відповідь.
- [x] **D3.** `LessonProgress` (userId × listingId × moduleIndex ×
      lessonIndex — лістинги адресуються позицією в JSON-масиві уроків,
      не окремим id, бо `curriculum` — суцільна JSON-колонка). Чекбокс
      «Переглянуто» на сторінці курсу, індикатор «Пройдено X з Y занять».
      `POST /me/library/:slug/progress` — той самий ґейт за `Entitlement`.
- [x] **D4.** Новий `MediaKind.gallery` — публічні зображення лістингу
      понад одну обкладинку. Розширено `ListingMediaManager` (секція
      «Додаткові зображення») і сторінку деталей каталогу (стрічка
      мініатюр). Завжди `access: public`, незалежно від того, що передав
      клієнт — та сама причина, що й для обкладинки.
- [x] **D5.** З `Рисунки/` зіставляється однозначно лише набір
      `30_Day_UAV_Engineering_Mastery_-_Slide_*.png` → курс `uav-mastery`
      (11 файлів реально існує, не 12 — Slide_11 відсутній). Автоматизовано
      в `prisma/seed.ts` (`seedUavGallery`): копіює файли в сховище,
      створює `MediaAsset` записи, ідемпотентно (пропускає, якщо галерея
      вже наповнена), і **не падає в проді** — тека `Рисунки/` існує лише
      на цій машині поза репозиторієм, перевірка `existsSync` пропускає
      крок, якщо її немає. Решту ~120 файлів (без чіткої прив'язки до
      курсу) заливаєш вручну через D4.

⚠️ **Чесно про захист відео:** YouTube unlisted — це приховування посилання,
не захист. Хто отримав лінк, дивитиметься завжди і зможе переслати. Гейтинг
у застосунку прибирає посилання з очей некупивших (і тепер — з самої
відповіді API, не лише з інтерфейсу), але не робить відео приватним. Якщо
колись знадобиться справжній захист — перезаливка на Mux/Vimeo Pro/
S3+CloudFront із підписаними URL; `youtubeEmbedUrl()` — єдине місце,
де провайдер конкретизується, тож заміна не зачепить решту D1–D3.

**Перевірено:** 6 нових e2e-тестів (стрипання videoUrl з публічної
відповіді, видача повної програми лише за наявності Entitlement,
незнайомець ніколи не бачить сутність, тогл прогресу в обидва боки з
ідемпотентним upsert, відмова в записі прогресу без покупки, публічність
gallery-зображень з розділенням від платних вкладень) + повний прогін —
усі зелені; typecheck/eslint/build обох застосунків чисті; вживу в
браузері підтверджено рендер 11 галерейних зображень на сторінці
`uav-mastery`.

---

### Фаза E — Підписки `L`

Блокує частину E-next і F5. У маркетплейсі **зараз немає жодної моделі
підписок** — тільки разові покупки (`Order` → `Payment` → `Entitlement`).

- **E1.** Моделі: `Plan`, `Subscription`, `SubscriptionPeriod`, `Quota`.
- **E2.** Рекурентні платежі LiqPay: продовження, скасування, невдале списання,
  грейс-період. Ідемпотентність колбеків — за наявним патерном `updateMany`-guard.
- **E3.** Плани для викладачів, письменників, експертів, маркетологів, студентів
  (склад тарифів — деталізуємо пізніше, за твоєю відповіддю).
- **E4.** Квоти й пільги в чекауті: «1 виріб на 3D-принтері на місяць»,
  «знижка 10% на курс» — взаємодія з наявними промокодами й loyalty-балами.
- **E5.** Сторінка тарифів (порт `pidpyska.html`).

---

### Фаза F — Нові домени зі старого сайту `L`

За твоєю командою «пропустити E, перейти до Фази F» — Фазу E (підписки) не
робили. F1 і F2 — завершено 2026-08-30.

- [x] **F1. Команди STEAM** — завершено, **без бейджа «Активна команда»**.
      Спочатку планував лишити F1 повністю невзятою через залежність бейджа
      від Фази E (див. історію рішень нижче), але за твоєю прямою командою
      «Давай фаза F» побудував решту F1 — вона не залежить від підписок:
      сутність команди (до 5 осіб включно з власником), запрошення
      зареєстрованих користувачів по email з підтвердженням/відмовою,
      модерація адміном перед публікацією, фото й результати роботи,
      каталог із фільтром за напрямком і пошуком за назвою. Бейдж лишається
      явно недобудованим — додати тривіально, коли зʼявиться модель
      підписки з Фази E (просто ще один прапорець на картці).
      `Team`/`TeamMember` (statuses: `pending`/`published`/`rejected` і
      `invited`/`confirmed`/`declined`), `GET /teams` (публічний каталог),
      `GET /teams/:id` (деталі, лише опубліковані), `POST /teams`
      (створення, гейт `RequireAuth`, чекбокс згоди на фото — обовʼязковий,
      `@Equals(true)`), `POST /teams/:id/invite` (лише власник),
      `GET /me/teams` / `GET /me/team-invites` + accept/decline,
      `POST /teams/:id/media` (фото/результати — перевикористано наявний
      `MediaAsset` з новим необовʼязковим `teamId`, kind `cover`/
      `attachment`, завжди `access: public`), адмінський `/admin/teams`
      (approve/reject із причиною). Нотифікації: `team_invited`,
      `team_submitted` (адмінам), `team_published`, `team_rejected`.
      **Ключове проєктне рішення:** місце в команді резервується вже в
      момент запрошення (`Team.memberCount`), не в момент підтвердження —
      інакше власник міг би розіслати необмежено запрошень, а прийняти
      могли б лише перші 5, залишивши підвішені запрошення, що виглядають
      дійсними. Guard-шаблон — точна копія
      `PromoCodesService.resolveForCheckout`/`ScheduleService.book`:
      `updateMany` з умовою `memberCount: {lt: 5}` в тій самій транзакції,
      що й створення рядка запрошення, і саме створення `create()` (не
      upsert) вперед гарантує, що повторне запрошення тієї самої людини
      падає на унікальному індексі до того, як гард на місткість взагалі
      побачить запит — тож не може подвійно зарезервувати місце вже зайнятій
      людині. Відмова від запрошення звільняє місце тим самим гардованим
      декрементом, що й скасування бронювання в Фазі F2.
      **Перевірено:** 13 нових e2e-тестів, зокрема справжній паралельний
      тест на ліміт 5 осіб (`Promise.allSettled` — два запрошення одночасно
      за останнє місце: рівно 1×201 і 1×409, `memberCount` фінально
      дорівнює 5). Повний прогін бекенду — **111 e2e-тестів у 14 наборах,
      усі зелені**, регресій немає; typecheck/eslint/build обох застосунків
      чисті; вживу в браузері (каталог, гейти `RequireAuth` на
      `/account/teams/new` і `/admin/teams`, обидві локалі, 404 на
      неіснуючу команду) — без помилок консолі.

  **Історія рішення:** первинно (до цієї команди) вирішив лишити F1 повністю
  невзятою, а не будувати частково без бейджа — «команди без ознаки
  ‘активна’ viewers не зрозуміють, що означає список». Це заперечення
  виявилось необґрунтованим: список команд і без бейджа читається як
  звичайний каталог (кожна картка вже показує напрямок, опис, кількість
  учасників) — бейдж був би додатковою ознакою, не обовʼязковою умовою
  розуміння екрана.
- [x] **F2. Бронювання занять** — завершено. **Реальне бронювання з
      контролем місткості**: `ClassSchedule`/`ClassBooking`, `GET /schedule`
      (публічний список майбутніх слотів), `POST`/`DELETE /schedule/:id/book`
      (запис/скасування, гейт `RequireAuth`), адмінський `/admin/schedule`
      (створення слотів, перегляд «4 з 12 місць», скасування слоту з
      нотифікацією всіх, хто записався). Нотифікації через наявний
      `NotificationsService` (`class_booked`, `class_cancelled`).
      **Знайдено й виправлено під час реалізації, до будь-якого запуску
      тестів:** наївна реалізація «інкремент місць потім upsert бронювання»
      давала подвійний інкремент місткості при повторному запиті того самого
      користувача на той самий слот (Postgres `upsert` — атомарний
      `INSERT ... ON CONFLICT`, він ніколи не падає на конфлікті, тож
      «інкремент, а тоді upsert» не міг відрізнити новий запис від
      повторного). Виправлено: спочатку атомарно «застовпити» рядок
      бронювання (`updateMany` з ґардом `status: {not: 'confirmed'}`, потім
      перевірка існування, і лише тоді `create`), і тільки після цього —
      гарантований одноразовий інкремент місткості. Лишкова гонка (два
      геть перших запити одного користувача одночасно) закрита через
      `try/catch` навколо транзакції з розпізнаванням P2002 по імені
      унікального індексу.
      **Перевірено:** 8 нових e2e-тестів, зокрема **справжній паралельний
      тест** (`Promise.allSettled` — 3 покупці одночасно за 2 місця на один
      слот: рівно 2×201 і 1×409, `bookedCount` фінально дорівнює 2) — не
      просто послідовна перевірка вичерпання місткості, а реальна гонка.
      Повний прогін бекенду — 98 e2e-тестів у 13 наборах, усі зелені,
      регресій немає; typecheck/eslint/build обох застосунків чисті; вживу
      в браузері (обидві локалі, порожній та гейтований стани) — без
      помилок консолі.
      Оновлено тимчасове рішення з Фази C: кнопка hero «Записатися на
      спільне заняття» тепер веде на реальний `/schedule` замість
      `/catalog?kind=course` (тимчасовий маршрут із C1 більше не потрібен).
- ➖ **F3.** Сторінка контактів із формою заявки — **не робили окремо**.
      Рішення (не було в плані, вирішив сам): реальна форма заявки на
      контакт із чекаутом на кшталт `kontakty.html` втратила сенс, коли
      з'явилось справжнє бронювання (F2) — `/schedule` вже і є той самий
      «зв'яжіться з нами щодо заняття», тільки з підтвердженням місця
      одразу, а не по пошті. Загальної форми «питання не про заняття»
      (напр. співпраця, медіа) не було в жодному з узгоджених пунктів
      плану — якщо вона таки потрібна окремо, дай знати.

---

### Фаза G — Злиття з Book_Creality `XL`

Варіант **Б**: Nova лишається окремим сервісом на своєму стеку
(Vite + Express + SQLite), але для користувача це один продукт.

- [x] **G1. Firebase Auth у Nova** — завершено й **змержено в master Nova**
      2026-08-29 після живої перевірки (див. «Перевірено» нижче). Замінили
      власну автентифікацію (scrypt-паролі, власний Google OAuth) на
      Firebase Auth того самого проєкту `fusionlab-acc2d`, що й у
      маркетплейсі.
      **Свідомо малий блаcт-радіус:** спочатку план описував це як
      «пронизує весь server.ts (3290 рядків) і 36 серверних модулів» —
      на практиці шар сесій/cookie (`nova_session`, `attachPrincipal`,
      `requireAuth`/`requireAdmin`/`requirePermission`) лишили
      незмінним, тож ~30 інших модулів і клієнтських файлів, що читають
      `req.principal` чи роблять `fetch('/api/...', {credentials:
      'same-origin'})`, **не чіпали взагалі**. Замінили лише сам крок
      підтвердження особи: клієнт входить через Firebase SDK (пошта/
      пароль або Google-спливне вікно, замість видалених
      `/api/auth/register`, `/api/auth/login`, `/api/auth/google[/callback]`),
      і обмінює виданий токен на ту саму cookie-сесію через один новий
      маршрут `POST /api/auth/firebase-session`.
      **Ключове рішення безпеки:** `findOrCreateFromFirebase` шукає
      користувача спершу за `firebaseUid`, і лише тоді — за поштою, причому
      прив'язку до наявного (ще не привʼязаного) акаунту дозволяє лише
      якщо Firebase підтверджує `email_verified`. Без цієї умови будь-хто
      міг би зареєструвати в Firebase чужу пошту (підтвердження не миттєве)
      і успадкувати чужий обліковий запис Nova з роллю й контентом. Вхід
      через Google завжди верифікований, тож реальний власник переходить
      прозоро; непідтверджена пошта, що збігається з наявним акаунтом —
      отримує явну відмову, а не тихе злиття. Такий самий клас ризику, що
      його `claimSeededAccount` у маркетплейсі закриває для власних сідів,
      тут написано заново під умови Nova (де немає концепції `seed:`).
      Таблиця `users` отримала необовʼязкову колонку `firebase_uid` (і в
      SQLite, і в JSON-фолбеку) — стара `password_hash`/`google_id`
      лишаються на прочитання наявних рядків, нові записи їх більше не
      пишуть (без міграції даних, per G7 нижче).
      **Перевірено:** переписано `scripts/test-auth.mts` навколо
      `findOrCreateFromFirebase` — 37/37, включно зі сценарієм спроби
      захоплення акаунту непідтвердженою поштою і підвищенням до `admin`
      за `ADMIN_EMAIL`. `test:sqlite` — 29/29 (нова колонка й індекс не
      зачепили наявну міграцію/CRUD). Typecheck і build — чисті. Живий
      запуск дев-сервера без Firebase-ключів підтвердив грациозну
      деградацію: `/api/auth/status`/`/api/auth/me` коректно повертають
      `firebaseEnabled: false`, а `POST /api/auth/firebase-session`
      відповідає 503 з поясненням замість падіння процесу.
      **Живу перевірку власником завершено 2026-08-29:** ключі перенесено
      в `.env` Nova, вхід адмінською поштою пройшов наскрізь (Firebase →
      обмін токена на сесію → сесія тримається → бейдж «Адміністратор
      сайту» в інтерфейсі, тобто роль `admin` призначилась автоматично за
      `ADMIN_EMAIL`). Гілку змержено в master.
      **Знайдено під час живої перевірки — не в коді автентифікації, але
      варте запису:**
      1. **Служба часу Windows (`w32time`) на цій машині вимкнена**, не
         просто розсинхронізована — `Start-Service w32time` падає з
         `0x80070426`. Годинник виправили разово, взявши час із заголовка
         `Date` HTTP-відповіді (`Set-Date`). Для Фази G це критично: без
         правильного годинника Firebase відхиляє ID-токени. Якщо годинник
         знову «втече» — або повторити цей же трюк, або таки полагодити
         службу (`Set-Service w32time -StartupType Manual`).
      2. **Конфлікт за порт 3000** зʼїв найбільше часу на перевірці й не
         мав жодного стосунку до G1: статичний сервер старого сайту
         (`npx serve -l 3000`), піднятий раніше в тій самій робочій сесії,
         змагався з Nova за той самий порт. Частина запитів потрапляла на
         Nova (тому вхід спрацював), частина — на `serve`, який віддавав
         свою сторінку «404 · The requested path could not be found». Це
         виглядало як «маршрут `/api/auth/me` не існує», хоча він
         реєструється першим. Урок на майбутнє (актуальний для G3, де
         зʼявиться reverse proxy): перш ніж дебажити маршрут, перевір,
         **хто саме** слухає порт — `Get-NetTCPConnection -LocalPort <порт>
         -State Listen` плюс `Get-CimInstance Win32_Process`.
      **Не перевірено вживу:** вхід через Google-спливне вікно саме зі
      сторінки Nova (перша спроба дала `auth/popup-blocked`, але то була
      стара вкладка маркетплейсу через той самий конфлікт портів, не
      Nova). Механізм спільний із входом поштою — відрізняється лише
      виклик Firebase SDK — тож ризик низький, але при нагоді варто
      клікнути кнопку «Увійти через Google» на самій Nova.
- **G2.** SSO: єдина сесія між маркетплейсом і Nova, спільний профіль
  користувача. `ADMIN_EMAIL` у Nova вже `tropazemli@gmail.com` — узгодити з
  роллю `admin` маркетплейсу.
  **Уточнено 2026-08-29 (після G3):**
  - *Єдина сесія* — після G3 обидва застосунки на одному origin, тож
    Firebase-стан у браузері спільний автоматично. Лишається дротування:
    при завантаженні Nova, якщо Firebase уже знає користувача, а серверна
    сесія ще гостьова — мовчки обміняти токен через наявний
    `POST /api/auth/firebase-session`, замість показувати екран входу.
  - *Спільний профіль* — **твоє рішення: звести до одного набору ролей**
    (не «маркетплейс як джерело правди» й не дві незалежні системи).
    Зараз набори різні й перетинаються лише на `admin` і `writer`:
    маркетплейс — `buyer/seller/admin/writer/expert/sales_manager/
    instruction_engineer/student`; Nova — `admin/writer/designer/
    translator/publisher/reader/guest`. Це найдорожчий варіант: міграція
    даних плюс зміни в обох кодових базах, включно з `BASE_SERVER_PERMISSIONS`
    і `utils/rbac.ts` у Nova та `src/auth/permissions.ts` у маркетплейсі.
    Перед реалізацією потрібен окремий раунд узгодження самого переліку —
    які саме ролі лишаються і що успадковують наявні акаунти.
- [x] **G3. Єдиний домен: Nova під `/studio`** — код готовий і перевірений
      локально наскрізь; лишається сам деплой (потребує твоїх рук, див.
      нижче). **Порядок змінено за твоїм рішенням: G3 перед G2**, і ось
      чому — браузер тримає стан входу Firebase **окремо для кожного
      origin**. Тобто «єдина сесія» з Фази G2 фізично неможлива, поки
      застосунки на різних адресах, хоч Firebase-проєкт і спільний. Префікс
      шляху робить їх одним origin, і після цього G2 зводиться до
      невеликого дротування замість міжоригінного обміну токенами, який
      довелося б потім викидати. Піддомен `studio.` не підійшов би — це
      теж окремий origin.
      **Як влаштовано:** rewrite у `next.config.ts` маркетплейсу
      (`/studio/:path*` → `NOVA_ORIGIN/:path*`, префікс **зрізається**),
      тож серверний бік Nova не змінився взагалі — Express і далі бачить
      `/api/...`. Змінився лише клієнт Nova: `vite.config.ts` бере
      `NOVA_BASE_PATH` для шляхів ассетів, а один перехоплювач `fetch`
      (`src/utils/basePath.ts`) додає префікс до звернень на `/api/`.
      Перехоплювач обрано свідомо замість правки 92 викликів у 32 файлах:
      вони записані і лапками, і шаблонними рядками, тож механічна заміна
      мала реальний шанс тихо зламати один із них, тоді як префікс — це
      властивість транспорту, а не кожного виклику. Без базового шляху
      перехоплювач не робить нічого.
      **Знайдено й виправлено під час реалізації (обидва — не з цих змін):**
      1. **Middleware `next-intl` перехоплював `/studio` раніше за rewrite**
         і намагався додати мовний префікс — сторінка віддавала 404 ще до
         того, як rewrite узагалі розглядався. Додав `studio` у виключення
         matcher поряд із `api`/`_next`. Перевірено, що власна локалізована
         маршрутизація маркетплейсу не постраждала.
      2. **Продакшн-збірку Nova взагалі не можна було запустити** — сервер
         збирався у CJS, хоча пакет `type: module`, тому `import.meta.url`
         виходив порожнім і `npm start` падав одразу на
         `fileURLToPath(undefined)`. Це блокувало б деплой як такий.
         Переведено на ESM (`dist/server.mjs`).
      ⚠️ **WebSocket свідомо не проходить через `/studio`:** rewrite Vercel
      не проксує upgrade-з'єднання, тож спільне редагування книг у Nova
      підключається напряму на її власний хост (`VITE_NOVA_WS_URL`), поки
      весь HTTP іде під спільним доменом. Крос-оригінний WebSocket — це
      нормально: правило одного походження на нього не діє, сервер сам
      перевіряє Origin.
      **Перевірено:** підняв Nova в продакшн-режимі й маркетплейс із
      `NOVA_ORIGIN` — `/studio` віддає екран входу Nova, ассети
      резолвляться під префіксом, а власний виклик клієнта
      `fetch('/api/auth/me')` іде як `/studio/api/auth/me` і повертає 200
      (це і доводить, що перехоплювач працює, не торкаючись жодного
      виклику). Регресію перевірено окремо: `/`, `/en`, `/catalog`,
      `/teams`, `/schedule` — усі 200. Typecheck/lint/build обох
      застосунків чисті, тести автентифікації Nova 37/37.
      **Потребує твоїх рук — власне деплой:**
      1. Задеплоїти Nova (Railway, як і API) — вона ніде не розгорнута.
         Змінні збірки: `NOVA_BASE_PATH=/studio/`, усі `FIREBASE_*` і
         `VITE_FIREBASE_*`, `VITE_NOVA_WS_URL=wss://<хост-Nova>/ws`.
      2. У Vercel-проєкті маркетплейсу додати `NOVA_ORIGIN=https://<хост-Nova>`
         і передеплоїти. Без цієї змінної rewrite просто не додається —
         локальна розробка й поточний прод працюють як раніше.
- **G4.** Публікація книг на вітрину стає **внутрішньою й безкоштовною** —
  замість мосту зі спільним секретом.
- **G5.** KDP/Etsy-інтеграції лишаються, але **як плагін для Pro/Ultra**
  підписників. Залежить від Фази E.
- **G6.** Власні AI-ключі користувачів (`apiKeysRoutes`) лишаються без змін —
  кожен письменник платить за свій AI.
- **G7.** Дані Nova (`nova-studio.db`) не мігруємо — старт з чистого аркуша.

---

## Порядок і залежності

```
A (ролі) ──┬── D (курси)
           ├── E (підписки) ──── G5 (KDP/Etsy для Pro/Ultra)
           ├── F1 (команди — без бейджа підписки, не блокується E)
           ├── F2 (бронювання)
           └── G1 (Firebase у Nova) ── G3 (спільний домен) ── G2 (SSO) ── G4

B (дизайн) — паралельно, не блокує нікого після B1–B4
C (hero)   — залежить лише від B1–B2 (токени й шрифти)
```

**G3 перед G2** (уточнено при реалізації, у початковому плані було навпаки):
стан входу Firebase браузер тримає окремо для кожного origin, тож єдина
сесія неможлива, доки застосунки на різних адресах. Спільний домен — це
передумова SSO, а не окрема косметична задача.

Фаза A йде першою і одна. Далі B і C можна вести паралельно з D.
E — лише перед G5 (F1 збудовано без бейджа активної підписки, тож більше не
залежить від E — див. Фазу F).

## Що пропоную відкласти

Ти обрав «реальний продукт важливіший, але портфоліо теж» і попросив назвати,
що ріжеться без болю. Мої кандидати:

1. **Інтерфейс конструктора курсів** (уже погоджено окремим пакетом) — експерт
   поки створює курс через наявну форму лістингу в кабінеті продавця.
2. ~~F1 «Команди STEAM»~~ — збудовано (без бейджа активної підписки), див.
   Фазу F. Виявилось, що не залежить від ядра маркетплейсу так, як здавалось
   спочатку — команда без бейджа все одно читається як звичайний каталог.
3. **G5 KDP/Etsy як платний плагін** — інтеграції вже працюють у Nova як є.
   Гейтинг за підпискою можна ввімкнути пізніше, коли Фаза E стабілізується.
4. **B5 (редизайн 27 сторінок)** — не відкладати цілком, а вести поступово:
   B1–B4 дають нову систему, сторінки переводимо хвилями.

Якщо погодишся хоча б на 1–3, обсяг першої ітерації падає приблизно на третину
без втрати робочого продукту.

## Ризики

- ~~G1 (Firebase у Nova) — найризикованіше в плані...~~ **закрито**:
  зроблено на окремій гілці, перевірено вживу власником і змержено в master
  Nova 2026-08-29. Ризик виявився суттєво меншим за оцінку: автентифікація
  не «пронизує» 36 модулів так, як здавалось до реалізації — сесійний
  cookie-шар лишився недоторканим, і жоден із модулів/клієнтських файлів,
  що читають `req.principal`, міняти не довелось.
- **Вільний вибір ролі** — див. попередження у Фазі A. Потребує рішення.
- **E2 (рекурентні платежі)** — LiqPay-підписки поводяться інакше за разові
  платежі; потрібні тести на всі стани (успіх, відмова, скасування, прострочення).
- **B5** — редизайн 27 сторінок легко ламає верстку в неочевидних місцях.
  Потрібен візуальний прохід по кожній сторінці в обох локалях.
- **Годинник хоста** — виправлено разово 2026-08-29, але **причина лишилась**:
  служба `w32time` на машині вимкнена й не стартує (`0x80070426`), тож
  `w32tm /resync` не спрацює. Годинник виставили з HTTP-заголовка `Date`
  (`Set-Date`). Очікуй, що він знову «втече» — і що це знову проявиться як
  незрозуміла відмова Firebase-токенів. Надійне рішення — полагодити службу
  (`Set-Service w32time -StartupType Manual; Start-Service w32time`).

## Відкриті питання

1. Права для ролей `instruction_engineer` і `student` — не визначені.
2. ~~Куди ведуть кнопки hero~~ — вирішено: «Записатися на спільне заняття» →
   реальний `/schedule` (Фаза F2, завершено). «Переглянути напрямки»
   лишається якорем на секцію категорій — окремої сторінки напрямків план
   не передбачає.
3. Склад тарифів підписок (E3) — домовились деталізувати пізніше. Досі
   відкрито: Фазу E пропущено за твоєю командою. F1 (команди STEAM) від неї
   більше не залежить (збудовано без бейджа активної командної підписки) —
   бейдж лишається одним рядком коду на потім, коли складеш тарифи.
4. Апрув ролей, повʼязаних із продажами — так чи ні (ризик у Фазі A).
5. Чи потрібна окрема форма "зв'яжіться з нами" поза бронюванням занять
   (напр. для співпраці/медіа-запитів) — F3 з початкового плану вважали
   поглиненою реальним бронюванням (Фаза F2), бо тодішня F3 була про запис
   на заняття, який тепер має справжній `/schedule`.
